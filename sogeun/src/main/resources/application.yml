spring:
  # DB 접속 정보 설정
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/sogeun
    username: root
    password:
  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

