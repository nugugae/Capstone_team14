spring:
  # DB 접속 정보 설정
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sogeun
    username: root
    password: Capst0ne!
  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
