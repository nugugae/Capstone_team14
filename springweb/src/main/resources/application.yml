spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        ddl-auto: update
    defer-datasource-initialization: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/capsuledb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: Capst0ne!


# H2 설정 제거
# h2:
#   console:
#     enabled: true
openai:
  api:
    model: ${OPENAI_MODEL:gpt-3.5-turbo}
    key: "${OPENAI_API_KEY:  }"
    url: "https://api.openai.com/v1/chat/completions"

jwt:
  secret_key: study-springboot
