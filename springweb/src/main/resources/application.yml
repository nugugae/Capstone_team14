#spring:
#  datasource:
#    url: jdbc:h2:mem:testdb
#    username: sa
#    password: password
#  jpa:
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#        ddl-auto: update
#  h2:
#    console:
#      enabled: true
#

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        ddl-auto: update
    defer-datasource-initialization: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sogeun?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password:


openai:
  api:
    model: ${OPENAI_MODEL:gpt-3.5-turbo}
    key: "${OPENAI_API_KEY: KEY}"
    url: "https://api.openai.com/v1/chat/completions"

jwt:
  secret_key: study-springboot